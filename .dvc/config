[core]
    remote = minio_remote
['remote "myminio"']
    url = s3://sample-data/data
    endpointurl = http://localhost:9000
    access_key_id = minio
    secret_access_key = minio123
['remote "minio_remote"']
    url = s3://sample-data
    endpointurl = http://localhost:9000
    access_key_id = minio
    secret_access_key = minio123
    region = us-east-1
